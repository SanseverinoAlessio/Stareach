import { PasswordRepeatValidator } from './password-repeat-validator';

describe('PasswordRepeatValidator', () => {
  it('should create an instance', () => {
    expect(new PasswordRepeatValidator()).toBeTruthy();
  });
});
