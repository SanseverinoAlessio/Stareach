import { TaskComplCounterPipe } from './task-compl-counter.pipe';

describe('TaskComplCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskComplCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
