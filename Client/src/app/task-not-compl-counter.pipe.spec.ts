import { TaskNotComplCounterPipe } from './task-not-compl-counter.pipe';

describe('TaskNotComplCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskNotComplCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
